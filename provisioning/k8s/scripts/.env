NS=senaev-com
VAULT_NS=vault

USER_ROOT_PATH="/home/ubuntu"

K3S_CLUSTER_PATH="$USER_ROOT_PATH/k3s-cluster"

PROVISIONING_PATH="$K3S_CLUSTER_PATH/provisioning"
SECRETS_PATH="$K3S_CLUSTER_PATH/secrets"
VOLUMES_PATH="$K3S_CLUSTER_PATH/volumes"

# ❗️ TODO: move to vault secrets
NOTES_FOLDER_PATH="$VOLUMES_PATH/webdav/data/notes-folder"
