
export default function Page() {

    return (
        <div>
            {'On this page, you can test difference between'}
            <code>{'fencedframe'}</code>
            {'and'}
            <code>{'iframe'}</code>
        </div>
    );
}
